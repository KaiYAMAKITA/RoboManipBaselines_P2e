## ~~バッファをどのように変更するか~~
一旦普通にフォルダにためて、os.removeする？

## ~~環境名の特定~~
どうにかした

## ~~P2Eでのenvの扱い方とrmbでのenvの扱い方に差異があるか（いままでP2Eを扱ってきたようにRMBでも使えるのか）p2e.train(env)のように~~


## ~~bin/Train.pyによってP2Eの学習を始めたい。リプレイバッファの作成が必要であるためRolloutを学習中に実行する必要がある。Rolloutする際には--checkpointの指定が必要だが、通常の場合とは違い、リプレイバッファ作成中はcheckpointを実行後に作成するため、最初はダミーのデータを入れることでパーサのrequiredを回避する必要があるー＞どうやって回避するか？~~
回避方法はわかった

## ~~同じく、複数クラスでパーサを利用する際に、parse_args()がそれぞれのクラスで実行されると、定義されていないArgumentに対してエラーが発生する。この問題はパースするときに認識していないオプションに対しては無視するという処理を加えれば問題なく実行できる（と思われる）が、どうしてもpolicy/p2e下以外のファイルの編集が必要になる。どうしよう~~

## checkpoint_dirにコマンドラインオプションで指定したcheckpointの値が入っているが、そんな実装はしていない
--checkpoint_dirと--checkpointというコマンドラインオプションがあるときに、--checkpoint ~~~という形で値を指定するとprefix abbreviation機能によって自動で--checkpoint_dirに値が格納されるらしい。知らなかった。

## how to execute
```
cd third_party
git clone git@github.com:kc-ml2/SimpleDreamer.git
```


```
python3 bin/Train.py P2e --policy P2e --env MujocoUR5eCable --dataset_dir dataset/MujocoUR5eCable100
```

## 学習データの利用方法について

### SimpleDreamer
500万ステップ分のデータを収集し、毎イテレーションバッチ数バッチサイズ分のデータをランダムに切り出してn回パラメータ更新学習

### RMBP2E
最初に大量のデータを収集しておき、そこからランダムに選択する形で学習、Nepochごとに追加でデータを収集して学習を行う。リプレイバッファに関しては溢れた分から削除していく


## データの違い

学習時はSimpleDreamer形式でもいいかも
ただRMB形式のデータセットをSimpleDreamer形式に変換する関数が必要
SimpleDreamerはactionで学習を行っているが、RMBはstateで学習を行っている

| 項目 | SimpleDreamer | RMB |
|------|----------------|-----|
| **データの種類** | observation, next_observation, action, reward, done | state, image (observation), action |
| **データ形式** | `torch.tensor` | `torch.tensor` |
| **モデルへの入力** | `AttrDict`型データをまるごと入力 | アンパックされたリストとして入力 |

### 流れ
- リプレイバッファ収集（RMB）（動的にデータセットが集まるかどうかーそんなことはないらしい）
- 学習（P2E）
- ロールアウト（RMB）


### 20251020
- RolloutでP2Eに必要な情報を取得できるかどうか確認
  - reward
  - できなければ追加で方法を考える
- P2Eとほぼ同じものをPolicyにて再現
  - P2Eのデータセットを扱う部分だけRMB仕様に変更することでこれを実現
- 学習部分の作成ー>rmb仕様にしたモデルの作成


# 以下memo

- Train.pyを実行したところcheckpointが要求される
  - RolloutBaseにあるargsの影響と思われる
  - checkpointに既存の別のパラメータを指定したところ、mkdirに関するエラーが出た。
    - RolloutBaseの到達する前に同エラーが出ていると思われるため、その前にもcheckpointを利用していると思われる。（argvが消えているので）
      - RolloutBase前でcheckpointが使われている場所を探す



- Train.py
  - TrainP2e.py
    - TrainBase.py
    - Rollout.py
      - RolloutP2e.py
        - RolloutBase.py


